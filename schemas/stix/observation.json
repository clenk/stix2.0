{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "observation",
  "description": "Observations document actions and objects that were observed at a certain time. The Observation object uses CybOX objects to describe something that was seen and is a container or wrapper (entry point) for CybOX data in STIX",
  "type": "object",
  "allOf": [
    {
      "$ref": "../common/core.json"
    },
    {
      "properties": {
       "type": {
          "type": "string",
          "description": "The type of this object, which MUST be the literal `observation`",
          "enum": ["observation"]
        },
        "start": {
          "$ref": "../common/timestamp.json",
          "description": "The time of the start of this observation."
        },
        "end": {
          "$ref": "../common/timestamp.json",
          "description": "The time of the end of this observation. For single point in time observations, this should match the start time."
        },
        "cybox": {
          "$ref": "../common/timestamp.json",
          "description": "The CybOX content that describes what was seen."
        }
      }
    }
  ],
  "required": ["start", "end", "cybox"]
}
