{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "granular-marking",
  "description": "The granular-marking type defines how the list of marking-definition objects referenced by the marking_refs property to apply to a set of content identified by the list of selectors in the selectors property.",
  "type": "object",
  "properties": {
    "selectors": {
      "type": "array",
      "description": "A list of selectors for content contained within the STIX object in which this field appears.",
      "items": {
        "type": "string",
        "minItems": 1
      }
    },
    "marking_refs": {
      "type": "array",
      "description": "The list of markings that apply to the fields selected by selectors.",
      "items": {
        "$ref": "identifier.json",
        "minItems": 1
      }
    }
  },
  "required": ["selectors", "marking_refs"]
}
